# Elastic Load Balancing
`Elastic Load Balancing` is the AWS service that automatically distributes in coming appication traffic across multiple resources.

A load balancer acts as a single point of contact for all incoming traffic to your `Auto Scaling Group`.
